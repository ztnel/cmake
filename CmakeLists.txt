cmake_minimum_required(VERSION 3.10)

# set the project name
project(Exponent VERSION 1.0)

configure_file(src/ExponentConfig.h.in src/ExponentConfig.h)

# target_include_directories(. PUBLIC "${PROJECT_BINARY_DIR}")

# add the executable
add_executable(exp src/exp.c)